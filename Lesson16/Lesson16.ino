// Lesson 16: More about for loops
int j = 0;
int dt = 500;
void setup() {
  Serial.begin(9600);
}

void loop() {
  // 1. Counting from 1 to 10
  //  for(j=0; j<10;j++){
  //    Serial.println(j);
  //    delay(dt);
  //  }
  //  Serial.println("I have exited the loop!");
  //  delay(dt*2);
  //  Serial.println("Going back in!");
  //  delay(dt*2);

  // 2. Counting from 5 to 10
  //    for(j=5; j<11;j++){
  //      Serial.println(j);
  //      delay(dt);
  //    }
  //    Serial.println("I have exited the loop!");
  //    delay(dt*2);
  //    Serial.println("Going back in!");
  //    delay(dt*2);

  // 3. Counting backwards
//  for (j = 10; j >= 1; j--) {
//    Serial.println(j);
//    delay(dt);
//  }
//  Serial.println("I have exited the loop!");
//  delay(dt * 2);
//  Serial.println("Going back in!");
//  delay(dt * 2);
}
